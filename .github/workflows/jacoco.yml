name: Run JaCoCo

on:
  push:

jobs:
  run-jacoco:
    name: '${{ matrix.os }}'
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-20.04, macos-12]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        ref: 2707b191c8b829e6bed41cd1ec7bcc9125e02af5
  
    - name: Set up JDK 8 for project
      uses: actions/setup-java@v4
      with:
        distribution: 'semeru'
        java-version: '8'

    - name: Overwrite old build.gradle file
      run: |
        curl -L -o newbuild.gradle https://pastebin.ai/download/bfpjx69omv
        mv newbuild.gradle build.gradle
    
    - name: Build project with JDK 8
      run: ./gradlew test jacocoTestReport

    - name: Upload JaCoCo reports
      uses: actions/upload-artifact@v4
      with:
        name: jacoco-report-${{ github.event.repository.name }}-on-${{ matrix.os }}
        path: 'build/jacocoHtml/*'
